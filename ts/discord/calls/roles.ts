
import DiscordServers = require('../../site/models/discord_servers');

import guildClient = require('../guildClient');

import * as Discord from 'discord.js';

function roleCreate(role: Discord.Role, discordClient: Discord.Client) {
	//
}

function roleDelete(role: Discord.Role, discordClient: Discord.Client) {
	//
}

function roleUpdate(oldRole: Discord.Role, newRole: Discord.Role, discordClient: Discord.Client) {
	//
}



export {
	roleCreate,
	roleDelete,
	roleUpdate
};
